/**
 * Engine identity for occupation wage profiles.
 *
 * Bump the version when the composition changes what a published figure means —
 * a different filing assumption, a different cost adjustment, a different way of
 * annualizing — so a result carrying an older version stays interpretable.
 */
export const OCCUPATION_WAGE_ENGINE_ID = 'occupation-wage-v1';
