# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*
!.env.example
data/**/.ingest.lock

# deployment and generated files
.vercel
next-env.d.ts
/dist/
/.wrangler/
/outputs/
/work/
*.tsbuildinfo

# The OEWS normalized snapshot is 12 MB — larger than the application itself.
# Its provenance lives in data/bls-oews/raw (the archives BLS served plus their
# digests) and its identity in data/bls-oews/manifest.json. Regenerate it with
# `npm run data:oews:rebuild`, which refuses to write a snapshot that does not
# hash to the promoted value.
data/bls-oews/snapshots/
